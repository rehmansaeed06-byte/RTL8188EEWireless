/* Alias header — gen_fw_blobs_rtl8188ee.py's emitted fw_blobs_rtl8188ee.c
 * includes "rtl8188ee_fw_blobs.h" (script/header naming mismatch from the
 * Feixiao fork, see scripts/gen_fw_blobs_rtl8188ee.py's header comment).
 * The real, hand-written struct definition lives in fw_blobs_rtl8188ee.h;
 * this file just forwards to it so both names resolve to one definition
 * instead of duplicating the struct in two places that could drift apart.
 */
#include "fw_blobs_rtl8188ee.h"
